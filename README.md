# FBee 桌宠框架

第一版 C# WPF / .NET 8 Windows 框架，包含透明置顶窗口、主显示器底部任务栏默认停靠、基础状态机、能量衰减与恢复、低能量睡眠、高能量待机后任务栏奔跑，以及睡眠点击唤醒。

资源目录：`Assets/animations/`、`Assets/audio/`。

在 Windows 上构建：`dotnet build FBee.csproj`；发布：`dotnet publish -c Release --self-contained true`。
