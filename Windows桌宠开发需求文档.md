# Windows 桌宠开发需求文档

## 项目概述

开发一个 Windows 桌面宠物程序，具备点击对话、抚摸交互、窗口内反弹、任务栏停靠、拖拽下落、蜜蜂追随、能量值与日常行为等核心功能。程序需以透明置顶窗口形式运行，支持单显示器，动画素材由用户提供（PNG 序列帧）。

## 技术选型要求

- 目标平台：Windows
- 窗口特性：无边框、透明背景、置顶、可拖拽移动
- 技术栈：C# + WPF（目标框架建议为 .NET 8 Windows）
- 选型理由：WPF 原生支持透明无边框窗口、置顶窗口、动画渲染和数据绑定；配合 Win32 API 可获取活动窗口、任务栏矩形、自动隐藏状态及鼠标位置，便于实现桌面物理交互；发布时可通过 .NET 自包含部署生成 Windows 可执行文件
- Windows API：用于获取活动窗口、任务栏状态、屏幕/DPI 信息及窗口位置
- 要求：支持 PNG 序列帧动画播放、音频多实例播放、Windows API 调用（获取活动窗口、任务栏状态等）

## 核心功能需求

### 0. 能量值与日常行为

- 桌宠拥有能量值，建议范围为 0-100，初始值和阈值可在设置中调整
- 桌宠处于清醒状态时，能量值随时间缓慢下降；下降速率应可配置，并支持暂停或特殊状态下不下降
- 能量值低于睡眠阈值（建议默认 20）时，待机状态自动切换为 `sleep` 动画
- `sleep` 为循环动画；睡眠期间不执行普通的 `run` 行为，点击、抚摸或拖拽等交互是否唤醒由设置项控制，默认建议唤醒
- 睡眠期间能量值缓慢恢复，达到唤醒阈值（建议默认 60）后回到 `idle`
- 每次有效点击桌宠都会恢复一定能量（恢复量可配置）；点击本身不立即结束睡眠
- 睡眠状态下累计有效点击达到设定次数（建议默认 3 次）后唤醒，能量值至少恢复到唤醒阈值或最低唤醒值
- 桌宠常态优先停靠在主显示器底部任务栏上边缘，播放 `idle` 动画；任务栏自动隐藏时解除停靠并执行普通桌面物理
- 当能量值较高（建议不低于 70），且在 `idle` 状态连续待机达到设定时长（建议默认 30-60 秒）后，触发 `run` 动画并沿底部任务栏水平奔跑；奔跑结束后回到任务栏上的 `idle`
- `run` 固定发生在主显示器底部任务栏区域内，不离开任务栏，不进入空中物理模式
- `run` 应设置单次或有限时长，避免持续奔跑导致桌宠长期离开任务栏；若奔跑过程中用户交互、拖拽或任务栏状态变化，则立即按状态优先级切换
- 能量值变化、睡眠和奔跑触发都必须纳入统一状态机，不能打断 `escape`、`fall` 等高优先级物理状态

### 1. 点击对话

- 点击桌宠时，随机播放一条语音（语音文件由用户提供，放在指定目录）
- 不显示任何气泡或文字
- 连续点击时，新的语音立即播放，不等待上一条结束
- 同时播放的语音数量限制为 4 条（可配置），超出后停止最早的一条
- 点击时播放“说话”动画（talk），语音结束后回到待机动画

### 2. 抚摸与部位点击

- 鼠标悬停在桌宠头部区域 **1 秒** 后触发抚摸动画（pat），循环播放，鼠标移开后停止
- 抚摸动画可伴随轻语音（如呼噜声）
- 点击不同部位触发不同反应（动画 + 随机语音）：
  - 点击头部：播放 click\_head 动画 + 随机语音
  - 点击身体：播放 click\_body 动画 + 随机语音
  - 点击脚部：播放 click\_foot 动画 + 随机语音
- 鼠标手型光标（悬停变为手型）作为后续优化项，当前版本可不实现

### 3. 窗口内部反弹

- 仅识别 **当前活动窗口**（非桌宠窗口）
- 只有将桌宠 **拖入** 该窗口内部后，窗口边缘才对桌宠产生碰撞反弹
- 进入后，桌宠被限制在窗口内部，像球一样在四边弹跳，不会飞出窗口
- 用户将桌宠拖出窗口，或窗口关闭/最小化/切换后，桌宠恢复普通桌面物理
- 窗口移动时，如果桌宠在内部，应跟随窗口移动（可选）

### 4. 底部任务栏停靠

- 仅支持 **底部任务栏**，并支持 **自动隐藏**
- 第一版仅支持单显示器和主显示器底部任务栏；不处理多显示器及顶部、左侧、右侧任务栏
- 程序启动且任务栏可见时，桌宠默认固定停靠在主显示器底部任务栏上边缘，作为常态待机位置
- 当任务栏可见时，桌宠被拖到任务栏上方附近（吸附范围约 20-30 像素），自动吸附并停在任务栏上边缘
- 常态停靠时播放 `idle` 动画，不受重力影响；如提供 `rest\_taskbar` 素材，可将其作为趴下/特殊休息姿态使用
- 用户拖走时解除吸附
- 任务栏自动隐藏时，解除吸附，桌宠恢复普通物理下落

### 5. 拖拽悬空与下落

- 按住桌宠开始计时，默认 **3 秒**（可在设置中调整）
- 超过 3 秒仍未松手：播放 **挣脱动画**（escape），随后桌宠从手中滑落并开始下落（即使鼠标仍按住）
- 未超过 3 秒松手：桌宠直接开始下落
- 下落过程中播放 fall 动画，受重力加速度影响
- 下落碰到 **普通应用窗口的顶部边缘** 时，向上反弹，反弹高度逐次衰减
- 下落到屏幕底部或任务栏上时停止，回到待机状态

### 6. 蜜蜂追随（奔跑/飞行）

- 检测鼠标左键拖拽方向（在桌面上按下并拖动，不检测真实图标）
- 触发后，桌宠变身蜜蜂形态，播放蜜蜂飞行动画
- 蜜蜂飞行动画只需要 **左右两个方向**（bee\_fly\_left / bee\_fly\_right）
- 实际移动方向可为任意角度：根据鼠标拖拽方向计算移动向量，桌宠沿该向量移动，同时根据水平方向选择左/右动画
- 拖拽结束或超时后停止，恢复原始形态待机

## 动画素材规格

- 格式：PNG 序列帧，带透明通道（RGBA）
- 画布尺寸：2048×2048 px（程序运行时建议缩放至 512×512 或 1024×1024 以减少内存占用）
- 帧率：12-15 fps（流畅动作可 24 fps）
- 命名规则：`动画名_方向_帧号.png`，例如 `bee_fly_left_000.png`
- 所有帧必须保持动作主体在画面中相对位置一致，避免播放抖动
- 循环动画首尾需衔接自然
- 动画素材本身不含阴影，阴影由程序绘制（半透明椭圆）

### 动画清单

| 状态       | 动画名称            | 循环/单次 | 方向    | 帧数建议  | 说明         |
| -------- | --------------- | ----- | ----- | ----- | ---------- |
| 待机       | idle            | 循环    | 正面    | 2-4 帧 | 呼吸、眨眼      |
| 说话       | talk            | 循环    | 正面    | 2-4 帧 | 嘴部动作，配合语音  |
| 被拖拽      | drag            | 循环/单次 | 正面    | 1-2 帧 | 被抓住姿态      |
| 悬空       | hover           | 循环    | 正面    | 2-4 帧 | 挣扎/悬浮      |
| 挣脱       | escape          | 单次    | 正面    | 2-4 帧 | 从手中滑落，衔接下落 |
| 下落       | fall            | 循环    | 正面    | 1-2 帧 | 下落姿态       |
| 反弹       | bounce          | 单次    | 正面    | 1-2 帧 | 碰到窗口顶部反弹瞬间 |
| 趴在任务栏    | rest\_taskbar   | 循环    | 正面/侧面 | 2-4 帧 | 休息/睡觉      |
| 睡眠       | sleep           | 循环    | 正面    | 2-4 帧 | 能量值过低时播放，睡眠期间恢复能量 |
| 奔跑       | run             | 单次/有限时长 | 左/右或正面 | 4-8 帧 | 高能量待机一段时间后触发，短距离奔跑 |
| 蜜蜂飞行（左）  | bee\_fly\_left  | 循环    | 左     | 4-8 帧 | 蜜蜂形态       |
| 蜜蜂飞行（右）  | bee\_fly\_right | 循环    | 右     | 4-8 帧 | 蜜蜂形态       |
| 抚摸反应     | pat             | 循环    | 正面    | 2-4 帧 | 享受/呼噜      |
| 点击头部     | click\_head     | 单次    | 正面    | 2-4 帧 | 开心/惊讶      |
| 点击身体     | click\_body     | 单次    | 正面    | 2-4 帧 | 痒/笑        |
| 点击脚部     | click\_foot     | 单次    | 正面    | 2-4 帧 | 生气/跳脚      |
| 点击尾巴（可选） | click\_tail     | 单次    | 正面/背面 | 2-4 帧 | 惊吓/尾巴竖起    |

## 开发阶段与验收标准

### 第一阶段：基础窗口与渲染

- 实现透明无边框置顶窗口，大小可设为 512×512（或根据动画缩放）
- 加载并播放待机动画（idle）
- 窗口可拖拽移动
- 验收：桌宠显示在屏幕上，播放待机动画，可拖动

### 第二阶段：动画系统

- 定义动画状态枚举，管理所有动画序列
- 支持循环动画和单次动画
- 单次动画播放后自动回到待机或指定状态
- 验收：可通过调试命令切换任意动画

### 第三阶段：点击与语音

- 检测鼠标点击事件
- 播放说话动画和随机语音
- 语音多实例叠加，限制最大同时数
- 验收：快速点击能叠加播放语音，动画正常

### 第四阶段：拖拽与物理

- 实现拖拽、悬空计时、挣脱、下落、屏幕底部停止
- 检测普通窗口顶部边缘并反弹（暂不限制在窗口内）
- 验收：拖走松手下落；按住 3 秒挣脱下落；碰到窗口顶部会反弹

### 第五阶段：任务栏停靠

- 获取底部任务栏矩形和自动隐藏状态
- 实现吸附、任务栏 `idle` 待机、解除吸附
- 验收：程序启动或拖到任务栏后停靠并播放 `idle`，任务栏隐藏后恢复普通物理

### 第六阶段：窗口内部反弹

- 获取当前活动窗口矩形
- 检测桌宠被拖入窗口内部，限制在窗口内弹跳
- 窗口关闭/切换后恢复
- 验收：拖入记事本窗口后在里面弹跳，拖出后正常下落

### 第七阶段：抚摸与部位点击

- 定义头部、身体、脚部点击区域
- 实现悬停头部 1 秒触发抚摸动画
- 实现不同部位点击反应
- 验收：抚摸和点击各部位均触发正确动画和语音

### 第八阶段：蜜蜂追随

- 检测鼠标拖拽方向，触发蜜蜂形态
- 播放左/右飞行动画，沿方向移动
- 拖拽结束恢复
- 验收：按住鼠标拖拽，桌宠变蜜蜂朝方向飞

### 第九阶段：能量值与日常行为

- 实现能量值模型、随时间衰减及配置持久化
- 实现低能量进入 `sleep`、睡眠恢复能量、达到阈值唤醒
- 实现高能量待机计时后触发 `run`，并在奔跑结束后回到任务栏 `idle`
- 验收：能量值降低到阈值后进入睡眠；睡眠期间能量恢复；高能量静置达到时长后触发奔跑；交互和物理状态可正确打断日常行为

### 第十阶段：设置与优化

- 右键菜单：设置、暂停动画、退出
- 设置项：音量、最大语音数、悬空时间、重力、反弹系数、能量衰减速率、睡眠阈值、唤醒阈值、高能量阈值、待机奔跑间隔、奔跑时长、动画开关
- 配置文件保存/读取
- 优化 CPU 和内存占用
- 验收：设置生效并持久化

### 第十一阶段：打包发布

- 打包为 Windows 可执行文件（exe）
- 添加图标和版本信息
- 测试不同分辨率（如 1920×1080、1366×768）
- 可加入开机启动选项

## 设置项要求

- 音量控制
- 最大同时播放语音数（默认 4）
- 悬空时间（默认 3 秒）
- 重力加速度
- 反弹系数
- 初始能量值和最大能量值
- 能量衰减速率
- 睡眠阈值和唤醒阈值
- 交互是否唤醒睡眠
- 高能量奔跑阈值
- 高能量待机触发时间
- 奔跑持续时间或奔跑距离
- 每次点击恢复的能量值
- 睡眠点击唤醒所需次数（建议默认 3 次）
- 是否启用窗口内部反弹
- 是否启用任务栏停靠
- 是否启用蜜蜂追随
- 是否开机启动

## 边界情况处理

- 任务栏自动隐藏：隐藏时解除吸附，桌宠下落
- 能量值过低：进入 `sleep`，睡眠期间不触发 `run`
- 清醒状态下点击：恢复可配置的能量值；睡眠状态下累计点击达到唤醒次数后才唤醒
- 能量值恢复到唤醒阈值：从 `sleep` 回到任务栏上的 `idle`
- 高能量奔跑时任务栏自动隐藏或用户开始交互：停止 `run`，按当前物理/交互状态处理
- 活动窗口最小化或关闭：窗口内部反弹状态自动解除
- 分辨率变化：窗口位置和任务栏区域需重新获取
- 音频文件缺失：播放默认提示音或跳过
- 动画素材缺失：使用占位动画或跳过

## 附加说明

- 动画素材由用户提供，按照命名规则放在 `assets/animations/` 目录下
- 语音素材放在 `assets/audio/` 目录下，支持 mp3/wav 格式
- 程序需处理鼠标穿透：仅桌宠本体可交互，透明区域不响应鼠标事件（后续可配置）
- 阴影由程序在桌宠下方绘制半透明椭圆，与动画分离
- 所有状态切换需平滑，避免突兀

## 交付要求

- 提供源代码和构建说明
- 提供可运行的 exe 文件
- 确保在 Windows 10/11 上运行正常
- 代码结构清晰，便于后续维护和扩展
