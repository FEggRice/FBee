<Window x:Class="FBee.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FBee"
        mc:Ignorable="d"
        Title="FBee" Width="256" Height="256"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" Topmost="True" ResizeMode="NoResize"
        MouseLeftButtonDown="OnMouseLeftButtonDown" MouseLeftButtonUp="OnMouseLeftButtonUp" MouseMove="OnMouseMove">
    <Grid Background="Transparent">
        <Ellipse Width="170" Height="34" Fill="#33000000" VerticalAlignment="Bottom" Margin="0,0,0,8" />
        <Border x:Name="PetBody" Width="150" Height="170" CornerRadius="75" Background="#FFF4C542"
                BorderBrush="#FF6B4E16" BorderThickness="3" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="●  ●" FontSize="28" HorizontalAlignment="Center" Foreground="#FF2B2115" />
                <TextBlock x:Name="StateText" Text="idle" FontSize="18" HorizontalAlignment="Center" Foreground="#FF2B2115" />
                <ProgressBar x:Name="EnergyBar" Width="100" Height="8" Margin="0,10,0,0" Minimum="0" Maximum="100" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
